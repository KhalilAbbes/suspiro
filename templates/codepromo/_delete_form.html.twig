


<form method="post" action="{{ path('codepromo_delete', {'id': codepromo.id}) }}" onsubmit="return confirm('Voulez-vous supprimer ce code Promo ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ codepromo.id) }}">
    <button type="submit" class="btn btn-fill btn-primary btn-sm me-2">Supprimer</button>
</form>
